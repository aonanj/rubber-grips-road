cmake_minimum_required(VERSION 3.16)
project(ConTemp LANGUAGES NONE)

message(FATAL_ERROR
    "Contact patch temperature system (ConTemp) now targets the Arduino Nano Every. "
    "Build and flash the sketch with the Arduino toolchain as described in README.md.")
